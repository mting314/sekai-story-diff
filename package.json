{
  "name": "sekai-story-diff",
  "private": true,
  "type": "module",
  "description": "Tooling repo root: conventional-commit enforcement. The site lives in web/.",
  "scripts": {
    "prepare": "husky || true"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "husky": "^9.1.7"
  }
}
