# Generated media (sprites, backgrounds, banners) lives in a bucket, not in the repo,
# so `bun run dev` and local builds resolve it from there too. Point this at a local
# path only if you have pulled the media down and want to work offline.
VITE_ASSET_BASE=https://storage.googleapis.com/sekai-story-diff-assets/
